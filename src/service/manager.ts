import { Configurable } from "@/artifacts";

/**
 * The Manager class is a configurable class that directs the service and the execution of jobs.
 * Responsibilities of the service manager:
 * - Issue and track identifiers for jobs, commands processes, and messages.
 */
class Manager
    extends
        Configurable
{
}
